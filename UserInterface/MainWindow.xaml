<Window x:Class="UserInterface.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:UserInterface"
        mc:Ignorable="d"
        Title="MainWindow" Height="316" Width="386">

    <Grid Margin="0,0,0,-6">
        <!-- Заголовок -->
        <TextBlock Text="Login to the Application"
            FontSize="20" FontWeight="Bold" Foreground="#000"
            HorizontalAlignment="Center" Margin="0,35,0,5"/>

        <TextBox 
            Width="250" 
            HorizontalAlignment="Center" 
            VerticalAlignment="Top" 
            Margin="0,95,0,0"
            x:Name="UserameTextBox"
            Text="Username" Height="30"/>
        <PasswordBox 
            Width="250" 
            HorizontalAlignment="Center" 
            VerticalAlignment="Top" 
            Margin="0,145,0,0"
            x:Name="PasswordBox" Height="30"/>
        <Button Content="Log in" 
            Width="200" Height="40" 
            HorizontalAlignment="Center" 
            VerticalAlignment="Top" 
            Margin="0,215,0,0" 
            Click="LoginButton_Click"/>
        <!-- Заголовок -->

        <!-- Поле для введення імені користувача -->
    </Grid>
</Window>
